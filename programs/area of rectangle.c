#include <stdio.h>

int main() {
    int length, breadth, area;

    printf("This is the program to calculate the area of a rectangle.\n");


    printf("Enter the length: ");
    scanf("%d", &length);

    printf("Enter the breadth: ");
    scanf("%d", &breadth);


    area = length * breadth;

    N
    printf("The area of the rectangle is %d\n", area);

    return 0;
}
